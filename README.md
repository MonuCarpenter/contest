How to use run script 

- run `./run <problem-name>` 
    if the folder exists it will compile and execute the code inside it else
  it will create a folder with the name provided and add a template code file inside it.

